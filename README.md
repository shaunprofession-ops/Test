Discussed the network architecture for onboarding two new AEP VDI applications. Both will be exposed over the internet, following the same pattern as LFI, secured via mTLS and/or IP whitelisting.
Details Requested:

Confirmation of internet connectivity for both AEP VDI apps (mTLS / IP whitelisting, same as LFI).
Network architecture diagram for both applications.
List of tools — to be shared by tomorrow.
